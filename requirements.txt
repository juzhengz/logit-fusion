# Pinned environment for this repository (Python 3.10)
# Install with:
#   pip install -r requirements.txt
#   pip install -e .

accelerate==1.12.0
datasets==4.4.1
transformers==4.57.3
torch==2.9.0

peft==0.18.0
deepspeed==0.18.2
wandb==0.23.1

math-verify==0.8.0
latex2sympy2_extended==1.10.2
num2words==0.5.14

Pillow==11.3.0
torchvision==0.24.0

# Optional heavy/runtime-specific extras. Uncomment only if needed.
vllm==0.11.2
bitsandbytes==0.48.2
